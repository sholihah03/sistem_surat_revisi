<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Image Driver
    |--------------------------------------------------------------------------
    |
    | Spatie Image uses the ImageMagick or GD PHP extensions to manipulate images.
    | The `gd` driver is the default and is more widely available. If you need
    | ImageMagick, you can change the driver to 'imagick'. For most use cases,
    | the `gd` driver will work fine.
    |
    | Supported: "gd", "imagick"
    |
    */

    'driver' => 'gd', // Menggunakan GD sebagai driver untuk manipulasi gambar

];
